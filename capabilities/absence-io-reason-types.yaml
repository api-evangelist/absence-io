naftiko: 1.0.0-alpha2
info:
  label: Absence.io API — Reason Types
  description: 'Absence.io API — Reason Types. 1 operations. Lead operation: Absence.io List Reason Types. Self-contained
    Naftiko capability covering one Absence Io business surface.'
  tags:
  - Absence Io
  - Reason Types
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ABSENCE_IO_API_KEY: ABSENCE_IO_API_KEY
capability:
  consumes:
  - type: http
    namespace: absence-io-reason-types
    baseUri: https://app.absence.io/api/v2
    description: Absence.io API — Reason Types business capability. Self-contained, no shared references.
    resources:
    - name: reasontypes
      path: /reasontypes
      operations:
      - name: listreasontypes
        method: POST
        description: Absence.io List Reason Types
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
  exposes:
  - type: rest
    namespace: absence-io-reason-types-rest
    port: 8080
    description: REST adapter for Absence.io API — Reason Types. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/reasontypes
      name: reasontypes
      description: REST surface for reasontypes.
      operations:
      - method: POST
        name: listreasontypes
        description: Absence.io List Reason Types
        call: absence-io-reason-types.listreasontypes
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: absence-io-reason-types-mcp
    port: 9090
    transport: http
    description: MCP adapter for Absence.io API — Reason Types. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: absence-io-list-reason-types
      description: Absence.io List Reason Types
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: absence-io-reason-types.listreasontypes
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
